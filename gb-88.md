<!-- 改编自：工具主题项目提示词/gb-88/gb-88.md；后端由 Node.js + Express + TypeScript 改造为 Go 1.22 + Gin + GORM -->

请根据以下要求生成项目代码和 README。

## ⚠️ 硬性约束（必须全部满足，否则视为项目不合格）

### 构建可运行性

- 前端执行 `npm install && npm run build`（或对应包管理器命令）必须零错误通过
- 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 必须零错误通过
- 所有 import/require 的模块必须在 go.mod / package.json 中声明
- TypeScript 项目不允许存在 any 类型逃逸导致的隐式运行时错误
- 数据库 Model/Schema 定义必须语法正确，GORM AutoMigrate 必须能成功执行

### Docker 一键启动

- `docker compose build` 必须成功完成，无报错
- `docker compose up -d` 启动后所有容器状态为 healthy/running
- 浏览器访问前端地址能看到完整可交互的页面（不是空白页或报错页）
- 后端 API 的健康检查接口（`GET /healthz`）返回正常响应

### 功能实现完整性

- 列出的每一项功能必须有完整的前后端实现代码，不能只有页面路由或空壳组件
- 核心流程必须端到端跑通：从用户操作 → 前端请求 → 后端处理 → 数据库读写 → 返回结果 → 前端渲染
- 不允许出现"点击按钮无反应"、"表单提交后无数据保存"、"列表页面显示为空因为没有接口"等情况
- 前端页面必须有完整的 UI 布局和交互逻辑，不能是只有标题和占位文本的空白页面

### 代码质量

- 所有文件使用 UTF-8 编码
- 不得有未使用的 import 或声明后未使用的变量（会导致构建警告或报错）
- API 接口的请求参数和响应格式必须前后端一致（类型定义对齐）
- 错误处理：网络请求失败时前端有提示，后端有统一错误响应格式

## 项目主要需求

### 在线API Mock服务器

**项目概述：** 在线 API Mock 服务管理平台，通过配置 Mock 规则模拟 API 响应，加速前后端并行开发。

**技术要求：**

- 前端：Vue 3 + TypeScript + Arco Design + Vite
- 代码编辑器：Monaco Editor
- 后端：Go 1.22 + Gin + GORM
- 数据库：MySQL 8.0（Mock 项目、接口、响应模板、请求日志均关系型存储，响应体使用 JSON 字段）
- Mock 引擎：github.com/brianvoe/gofakeit（Go 版 Faker，用于生成随机但真实的数据）

**核心流程（必须端到端完整实现并可验证）：**

1. **创建 Mock 项目**：新建项目（如"用户服务"） → 生成项目 Base URL（如 `http://localhost:3119/mock/{projectId}`）
2. **配置接口**：在项目下添加接口 → 设置路径 `/api/users`、方法 GET、状态码 200 → 配置响应 Body（JSON）
3. **发送 Mock 请求**：GET `http://localhost:3119/mock/{projectId}/api/users` → 返回配置的 JSON 响应
4. **动态响应**：使用 gofakeit 模板 `{"name":"{{name.fullName}}","email":"{{internet.email}}"}` → 每次请求返回随机但真实的数据
5. **条件响应**：配置规则：当 query 参数 `role=admin` 返回管理员数据，否则返回普通用户数据
6. **请求日志**：查看所有发到 Mock 服务的请求记录（方法、路径、Headers、Body、时间）

**增强功能（在核心流程完成后实现）：**

1. Swagger 导入：上传 OpenAPI 2.0/3.0 的 JSON 文档，自动解析生成 Mock 项目下的接口与响应模板
2. 响应延迟模拟：为接口配置响应延迟（毫秒），发送 Mock 请求时按配置延迟后返回

**测试样例数据：**

- 预置 2 个用户：dev/dev123、lead/lead123
- 预置 1 个 Mock 项目「用户管理系统」包含 5 个接口：
  - GET /api/users → 返回用户列表（gofakeit 生成 5 个随机用户）
  - GET /api/users/:id → 返回单个用户详情
  - POST /api/users → 返回创建成功的用户（包含 id 和 createdAt）
  - PUT /api/users/:id → 返回更新成功的响应
  - DELETE /api/users/:id → 返回 204 No Content
- 预置 10 条请求日志记录

## 技术要求

- **英文短名（用于 Docker Compose）**：`apimock`
- **前端端口**：`FRONTEND_PORT=8119`
- **后端端口**：`BACKEND_PORT=3119`（本地）/ `8080`（Docker 内部）
- **前端框架**：Vue 3 + TypeScript + Arco Design + Vite
- **代码编辑器**：Monaco Editor
- **后端框架**：Go 1.22 + Gin（github.com/gin-gonic/gin）
- **ORM**：GORM（gorm.io/gorm + gorm.io/driver/mysql）
- **数据库**：MySQL 8.0
- **认证方式**：JWT（github.com/golang-jwt/jwt/v5）+ RBAC；密码哈希 golang.org/x/crypto/bcrypt
- **Mock 引擎**：github.com/brianvoe/gofakeit（动态/条件响应数据生成）
- **API 文档**：github.com/swaggo/gin-swagger（挂载于 `/docs`）
- **代码规范**：严格遵循单一职责原则，每个文件只负责一个功能；常量（错误码、配置项）统一维护在 `backend/internal/constants/` 目录下；日志统一使用 `log/slog`，日志配置在 `backend/internal/logger/logger.go`；异常信息统一定义在 `backend/internal/constants/error_codes.go`，后端通过 Gin 中间件统一捕获并返回标准 JSON 格式响应（`{code, message, data}`）。

## 后端目录结构（Go 标准布局，强制执行）

后端代码放在 `backend/`，数据库脚本放在 `backend/database/`：

```
backend/
├── go.mod
├── go.sum
├── Dockerfile
├── cmd/
│   └── server/
│       └── main.go
├── database/
│   └── init.sql          # 建表 SQL + 种子数据（预置用户/Mock 项目/请求日志）
└── internal/
    ├── config/           # 配置加载（MySQL、JWT、Server）
    ├── model/            # GORM 模型：user.go, project.go, endpoint.go, response_template.go, request_log.go
    ├── repository/       # 数据访问：user_repository.go, project_repository.go, endpoint_repository.go, request_log_repository.go
    ├── service/          # 业务逻辑：auth_service.go, project_service.go, endpoint_service.go, mock_engine.go（动态/条件响应）, request_log_service.go
    ├── handler/          # HTTP 处理：auth_handler.go, project_handler.go, endpoint_handler.go, mock_handler.go（/mock/{projectId}/...）, request_log_handler.go
    ├── router/           # 路由：router.go, auth.go, project.go, endpoint.go, mock.go, request_log.go
    ├── middleware/       # auth.go, error_handler.go, request_logger.go, rate_limiter.go
    ├── dto/              # 请求/响应结构体：auth_dto.go, project_dto.go, endpoint_dto.go
    ├── constants/        # error_codes.go, messages.go
    ├── util/             # jwt.go, response.go, faker.go（gofakeit 模板渲染）
    └── logger/           # logger.go（log/slog 配置）
```

后端本地构建与运行命令：
- 依赖安装：`go mod tidy`
- 编译检查：`go build ./...`
- 本地运行：`go run ./cmd/server`

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：
页面、组件、路由、状态管理、API 请求封装、类型定义、样式和资源等放在对应的子文件夹。

后端代码放在 `backend/`：
模型、数据访问、业务逻辑、HTTP 处理、路由、中间件、工具函数等按职责分层存放（见上方 Go 标准布局）。

数据库脚本放在 `database/`：
包含建表 SQL 或 GORM AutoMigrate 迁移文件，以及种子数据（seed）脚本。

## 部署要求

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=apimock`
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

backend/Dockerfile 使用以下 Go 多阶段构建骨架：

```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：

- **不要写 `version:` 字段**
- 顶层声明 `name: apimock`
- 所有服务使用命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-apimock}` 前缀
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API（`/api` 转发到 `http://backend:8080/`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 healthcheck，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 端口映射（本项目分配的端口）：
  - 前端：`${FRONTEND_PORT:-8119}:80`
  - 后端：`${BACKEND_PORT:-3119}:8080`
  - MySQL：`${DB_PORT:-3306}:3306`
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=apimock`
- 数据库连接相关：`DB_NAME`、`DB_USER`、`DB_PASSWORD`、`DB_ROOT_PASSWORD`
- 后端：`JWT_SECRET`
- 端口映射：`FRONTEND_PORT`、`BACKEND_PORT`、`DB_PORT`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

README 中务必告知用户首次启动前执行 `cp .env.example .env`。

### 构建验证清单（生成代码后必须逐项验证）

1. `docker compose build` — 三个镜像全部构建成功，无报错
2. `docker compose up -d` — 所有容器启动，`docker compose ps` 显示 healthy/running
3. 浏览器访问 `http://localhost:8119` — 看到完整的应用首页
4. 前端页面上的核心功能按钮/链接全部可点击且有响应
5. 前端发起的 API 请求在浏览器 Network 面板中返回 200
6. 数据库中已有种子数据（见上方「测试样例数据」）
7. 在 `backend/` 下执行 `go mod tidy`、`go build ./...` 均无错误

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令，放在最前面）
- 本地开发方式（前端启动命令、后端启动命令：`cd backend && go mod tidy && go run ./cmd/server`）
- 访问地址
- 技术栈（表格展示，后端技术栈写 `Go 1.22 + Gin + GORM`）
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- 测试账号（dev/dev123、lead/lead123）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

## Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配（至少包含）：

```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
coverage.out
frontend/coverage/
```
